// Placeholder for custom fields helpers
// Users don't have custom fields in the current schema

export const fetchCustomFieldsForUser = async (userId) => {
  // Placeholder
  return [];
};

export const mapCustomFieldsToFormValues = (user, customFields) => {
  // Placeholder
  return {};
};

export const updateUserCustomFields = async (userId, customFieldsData) => {
  // Placeholder
  return {};
};